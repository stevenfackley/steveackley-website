# =============================================================================
# steveackley.org — Local Development Docker Compose
# Runs PostgreSQL only; Next.js runs locally with `npm run dev`
# =============================================================================

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: steveackleydb
      POSTGRES_USER: steveackley
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U steveackley -d steveackleydb"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_dev_data:
    driver: local
